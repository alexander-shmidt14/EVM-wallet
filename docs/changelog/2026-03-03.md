---
tags: [changelog]
last_updated: 2026-03-03
---

# 2026-03-03 — Electron Auto-Update System

**Раздел:** [[changelog/_index|Changelog]] · **Главная:** [[_index]]

---

## Added

### Auto-Update Feature
- ✨ Система автоматических обновлений через `electron-updater` + `electron-log`
- ✨ Module: `apps/desktop/src/backend/auto-updater.ts` — управление жизненным циклом обновлений
- ✨ Проверка обновлений при старте приложения (только в production)
- ✨ Периодическая проверка каждые 30 минут
- ✨ User dialogs: "Обновление доступно?" → "Готово к установке?"
- ✨ Progress tracking: taskbar progress bar + IPC event `update-progress` для renderer
- ✨ Graceful error handling: логирование ошибок без крэша приложения

### Build & Release
- 📦 Dependencies: добавлены `electron-updater@^6.1.0`, `electron-log@^5.0.0` в `apps/desktop`
- 📦 Publish config: добавлены блоки `publish` в `apps/desktop/electron-builder-nosign.json` и `apps/desktop/package.json`
  - Provider: GitHub
  - Token: `${GH_TOKEN}` из environment
  - Генерация `latest.yml` при сборке для auto-updater
- 🔄 GitHub Actions: обновлен workflow `.github/workflows/windows-build.yml`
  - Флаг `--publish always` при push тега `v*`
  - Переменная `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`
  - Отключена double-upload через softprops/action-gh-release (electron-builder публикует напрямую)

### IPC API
- 🔌 Preload API расширена методом `onUpdateProgress(callback)` для подписки на события скачивания

### Version
- 📌 Desktop app version обновлена: **1.0.0 → 1.1.0**

## Documentation

### New Pages
- 📖 **`docs/devops/auto-update.md`** — полная документация системы авто-обновлений
  - Architecture diagram
  - Flow для клиентов
  - Troubleshooting guide
  - Версионирование и логирование

### Updated Pages
- 📖 **`docs/devops/_index.md`** — добавлена ссылка на новую страницу auto-update
- 📖 **`docs/devops/windows-build.md`**
  - Обновлен диаграмма (mermaid flowchart) с publish flow
  - Добавлена секция "Publish to GitHub" с `--publish always` и `GH_TOKEN`
  - Обновлены related_files
  - Обновлена дата last_updated
- 📖 **`docs/devops/release.md`**
  - Секция "Автоматическая публикация" расширена информацией о `latest.yml` и blockmap
  - Добавлена информация о клиентском развёртывании через auto-update
  - Версия обновлена: 1.0.0 → 1.1.0
  - Добавлена ссылка на auto-update doc
- 📖 **`docs/backend/electron-main.md`**
  - Обновлена диаграмма жизненного цикла (добавлена ветка `initAutoUpdater`)
  - Добавлена новая секция "Инициализация Auto-Updater"
  - Обновлены related_files
  - Добавлена ссылка на auto-updater.ts в "See also"

## Developer Experience

- 🎯 Auto-updater инициализируется автоматически в production
- 🎯 Development mode: updater отключен (ускорение разработки)
- 🎯 Логирование через `electron-log` с полной диагностикой
- 🎯 Progress events доступны для кастомного UI (если потребуется позже)

## Testing

При релизе `v1.1.0`:
1. Тег `v1.1.0` pushится в GitHub
2. Windows Build запускается с `--publish always`
3. electron-builder:
   - Генерирует `latest.yml` (версия 1.1.0)
   - Загружает `.exe` и `latest.yml` в GitHub Release
4. Клиенты с версией 1.0.0:
   - Проверяют GitHub Releases
   - Обнаруживают версию 1.1.0
   - Видят диалог обновления
   - Скачивают, устанавливают, перезагружаются

---

## Files Changed

```
apps/desktop/package.json                               (+3 deps, +version, +build.publish)
apps/desktop/src/backend/auto-updater.ts              (NEW, 180 строк)
apps/desktop/src/backend/main.ts                       (+import, +initAutoUpdater call)
apps/desktop/src/backend/preload.ts                    (+onUpdateProgress method)
apps/desktop/electron-builder-nosign.json              (+publish block)
.github/workflows/windows-build.yml                    (--publish always, GH_TOKEN)
docs/devops/auto-update.md                             (NEW, 250+ строк)
docs/devops/_index.md                                  (+auto-update ссылка)
docs/devops/windows-build.md                            (publish diagram, GH_TOKEN docs)
docs/devops/release.md                                  (latest.yml, auto-update info, version bump)
docs/backend/electron-main.md                           (lifecycle diagram, auto-updater section)
```

---

## Notes

- 🔒 **Private repo**: GitHub Release остаются публичными для скачивания (простой доступ без авторизации)
- 🔐 **GH_TOKEN**: используется только на CI (secrets не экспортируются)
- ✅ **Backward compatible**: старые версии приложения (< 1.1.0) не ломаются
- 📊 **Telemetry**: логи обновлений сохраняются в `~/.config/EVM Wallet/logs/` для диагностики

---

## Related Docs

[[devops/auto-update|Auto-Update System]] · [[devops/windows-build|Windows Build]] · [[devops/release|Релиз]] · [[backend/electron-main|Electron Main]]
